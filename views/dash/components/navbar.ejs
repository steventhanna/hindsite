<nav class="navbar navbar-default hidden-xs navbar-fixed-top">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="#"><%= dash.name %></a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right">
        <li class="dropdown-menu" <% if (currentPage == "monitors") { %> active" <% } else { %>"<% } %>>
          <a id="monitors" href="/monitors" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
            Monitors <span class="caret"></span></a>
            <ul class="dropdown-menu">
              <% for (var i = 0; i < monitors.length; i++) { %>
                <li>
                  <a href="/monitors/view/<%= monitors[i].id %>"><%= monitors[i].name %></a>
                </li>
              <% } %>
            </ul>
        </li>
        <li <% if (currentPage == "incidents") { %> class="active" <% } %>>
          <a id="incidents" href="/incidents">Incidents</a>
        </li>
        <li <% if (currentPage == "integrations") { %> class="active" <% } %>>
          <a id="integrations" href="/integrations">Integrations</a>
        </li>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><%= user.fullName %> <span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li <% if (currentPage == "settings") { %> class="active" <% } %>>
              <a id="settings" href="/settings">
                <i class="fa fa-gear"></i>
                Settings</a>
            </li>
            <li><a href="/logout"><i class="fa fa-power-off"></i> Sign Out</a></li>
          </ul>
        </li>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
